# Archived migrations

These files were split into incremental migrations during development. They are **superseded** by [`../setup.sql`](../setup.sql) for new projects.

Keep them for reference or partial upgrades on existing databases. Run in filename order if you need a specific module only.
