VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Local dev only (default /). GitHub Pages CI sets /repo-name/
# VITE_BASE_PATH=/

# Allow sign-up UI locally while bootstrapping. Production CI sets false.
VITE_ALLOW_SIGNUP=true
